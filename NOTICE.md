# NOTICE

## 数据版权

- 《类似商品和服务区分表》© 国家知识产权局商标局（中国商标网 sbj.cnipa.gov.cn 发布）。
- 《尼斯分类》（Nice Classification, NCL）© 世界知识产权组织（WIPO）。

本包是上述官方公开出版物的事实性条目汇编（编码 + 名称），供内部商标管理系统的分类检索使用；不替代官方文本，以官方发布为准。

## 提取说明

- 基础数据：官方 2025 文本 PDF（扫描版）的视觉识别转录（大类 45、群组 492、项目 12,037，
  同码同义词行聚合为一条，名称以全角逗号连接）；
- 增量：官方 NCL13-2026 修改内容（文本版 PDF）经 `scripts/apply-revision.ts` 程序化套用
  （删除 59、改名 61、新增核对），`version` 字段区分来源（`2025` / `2026`）；
- 结构校验（编码唯一、群组前缀=大类、45 类齐全）由 `nice.test.ts` 钉死。

### 已知局限

项目编号是大类内全局顺序号，前 4 位不等于群组（`parentCode` 来自印刷版式）。
数据源为纯扫描件：两次独立的视觉提取在行级配对上互有漂移（编码列漏读一行即整体错位），
本包以较早一次的完整提取为基准并套用文本版修订；个别名称可能存在 OCR 字误
（如已修正 白肮→白朊）。`scripts/cross-check.ts` 保留了两套提取的对照方法。
后续若中国商标网发布文本版完整区分表，建议用 `scripts/` 流水线重新生成以消除残差。
